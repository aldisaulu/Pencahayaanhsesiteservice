# TODO - Pencahayaan (PT GNI)

- [x] Update service worker cache name to bust old assets (v2).
- [x] Ensure script.js validates jsPDF presence and logs diagnostic details when library fails.
- [ ] Fix APP_SHELL list in sw.js (properly include local libs paths) and remove syntax/format issues.
- [ ] Update index.html script includes to match service-worker cached asset list (optional but recommended).
- [ ] Test workflow: open page -> enter values -> export PDF offline/online.
- [ ] Test workflow: export Excel (CSV) still works after edits.

